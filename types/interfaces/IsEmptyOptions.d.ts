/**
 * Empty validate option.
 */
export default interface IsEmptyOptions {
    /**
     * If true, whitespace before or after the string is ignored. Default is false.
     */
    ignoreWhitespace?: boolean;
}
