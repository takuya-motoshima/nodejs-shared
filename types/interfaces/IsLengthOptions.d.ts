/**
 * Length validate option.
 */
export default interface IsLengthOptions {
    /**
     * Minimum length. Default is 0.
     */
    min?: number;
    /**
     * Maximum length. Default is none (undefined).
     */
    max?: number;
}
